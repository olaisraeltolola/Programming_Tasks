import java.util.Scanner;
	public class Test2{
		public static void main(String[] args){
		Scanner input = new Scanner(System.in);
		
		System.out.println("Enter the number (1-30)");
		int numberOne = input.nextInt();
	if (numberOne < 1 | numberOne > 30)
		System.out.print("Number cannot be inputted, didn't you read the instruction?")
	else 
		
		System.out.println("Enter the number (1-30)");
		int numberTwo = input.nextInt();
	if (numberTwo < 1 | numberTwo > 30)
		System.out.print("Number cannot be inputted, didn't you read the instruction?")

		System.out.println("Enter the number (1-30)");
		int numberThree = input.nextInt();
	if (numberThree < 1 | numberThree > 30)
		System.out.print("Number cannot be inputted, didn't you read the instruction?")

		System.out.println("Enter the number (1-30)");
		int numberFour = input.nextInt();
	if (numberFour < 1 | numberFour > 30)
		System.out.print("Number cannot be inputted, didn't you read the instruction?")

		System.out.println("Enter the number (1-30)");
		int numberFive = input.nextInt();
	if (numberFive < 1 | number Five > 30)
		System.out.print("Number cannot be inputted, didn't you read the instruction?")

		int sum = numberOne
}